# Spam Email Classifier
Simple Naive Bayes classifier to detect spam vs ham using TF-IDF features.

## Files
- `sample_emails.csv` - small dataset
- `spam_classifier.py` - training and evaluation script
- `requirements.txt` - dependencies

## How to run
```bash
pip install -r requirements.txt
python spam_classifier.py
```
